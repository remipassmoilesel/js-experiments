# Upgrade de version Node

En utilisant cette méthode, l'utilisation de sudo devient obligatoire pour 
toutes opérations d'écriture (installation de paquets, etc ...)

Utiliser 'n':

    $ sudo apt install nodejs
    $ sudo npm install -g n
    
Changer de version:
    
    $ sudo n stable
    $ sudo n latest
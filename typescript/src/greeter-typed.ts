(() => {

    function greeter3(person: string) {
        return "Hello, " + person;
    }

    // error here, type incorrect
    // var user = [0, 1, 2];
    // document.querySelector("#out3").innerHTML = greeter3(user);

})();

{
  "name": "vue-typescript-experiments",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "node_modules/.bin/rimraf dist",
    "compile": "node_modules/.bin/tsc --pretty",
    "compile-watch": "node_modules/.bin/tsc --pretty -w",
    "clean-compile": "npm run clean && npm run compile",
    "test": "npm run clean-compile && node_modules/.bin/mocha 'build/tests/**/*Test.js'",
    "pack": "npm run clean && node_modules/.bin/webpack",
    "pack-watch": "npm run clean && node_modules/.bin/webpack  --progress --colors --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.4",
    "vue": "^2.5.2"
  },
  "devDependencies": {
    "@types/lodash": "^4.14.78",
    "@types/vue": "^2.0.0",
    "ts-loader": "^3.0.5",
    "tslint": "^5.8.0",
    "typescript": "^2.5.3",
    "webpack": "^3.8.1"
  }
}
